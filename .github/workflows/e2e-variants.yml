name: E2E (Harness variants)

on:
  pull_request:
    paths:
      - src/**
      - crates/**
      - scripts/e2e-variants.sh
      - .github/workflows/e2e-variants.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: e2e-variants-${{ github.ref }}
  cancel-in-progress: true

jobs:
  live:
    if: >-
      github.repository == 'nickderobertis/oneharness' &&
      (github.event_name != 'pull_request' ||
       github.event.pull_request.head.repo.full_name == github.repository)
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            run_opencode: "1"
            run_qwen: "1"
          - os: macos-latest
            run_opencode: "1"
            run_qwen: "0"
          - os: windows-latest
            run_opencode: "0"
            run_qwen: "1"
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    defaults:
      run:
        shell: bash
    env:
      OH_E2E_NO_SKIP: 1
      OH_E2E_VARIANTS_API_ONLY: 1
      OH_E2E_VARIANTS_RUN_OPENCODE: ${{ matrix.run_opencode }}
      OH_E2E_VARIANTS_RUN_QWEN: ${{ matrix.run_qwen }}
    steps:
      - uses: actions/checkout@v4
      - name: Verify API identities
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          [ -n "${ANTHROPIC_API_KEY:-}" ] || { echo "::error::ANTHROPIC_API_KEY is missing"; exit 1; }
          [ -n "${OPENAI_API_KEY:-}" ] || { echo "::error::OPENAI_API_KEY is missing"; exit 1; }
      - uses: actions-rust-lang/setup-rust-toolchain@v1
      - uses: taiki-e/install-action@v2
        with:
          tool: just
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
      - name: Install real CLIs
        run: bash scripts/live-variants-tools-e2e.sh
      - name: Live API variants and same-harness fallback
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: just live-variants
